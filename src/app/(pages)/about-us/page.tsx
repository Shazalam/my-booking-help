import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About us",
  description: "about us ",
};

const AboutUS = () => {
  return (
    <div>
      <h1>About us</h1>
    </div>
  );
};

export default AboutUS;
