import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact",
  description: "Contact",
};

const Contact = () => {
    return (
      <div>
        <h1>contact</h1>
      </div>
    );
  };
  
  export default Contact;
  